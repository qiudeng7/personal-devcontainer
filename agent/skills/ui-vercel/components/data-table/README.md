# Data table contract

Use a semantic table for comparable records. The row's primary action is a real link or button;
do not make the entire row clickable unless it has one unambiguous destination. Keep status and
actions in dedicated columns.
