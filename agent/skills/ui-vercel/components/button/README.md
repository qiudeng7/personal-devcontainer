# Button contract

Use a native `<button>` for actions and an `<a>` only for navigation. Variants are `primary`,
`secondary`, `quiet`, and `danger`. Keep labels action-specific. Disabled controls use the native
`disabled` attribute and never imitate availability with opacity alone.
