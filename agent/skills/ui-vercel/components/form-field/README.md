# Form field contract

Every control has a persistent label. Helper and error text are connected with `aria-describedby`.
Use a native checkbox for form submission; the button-style switch is reserved for settings that
take effect immediately and exposes `role="switch"` plus `aria-checked`.
